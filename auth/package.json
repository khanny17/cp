{
    "main": "index.js",
    "scripts": {
        "start": "micro",
        "deploy": "now && now alias",
        "dev": "micro-dev -p 3001"
    },
    "dependencies": {
        "bcrypt": "^3.0.0",
        "jsonwebtoken": "^8.3.0",
        "micro": "^9.3.2",
        "micro-cors": "^0.1.0",
        "micro-jwt-auth": "^1.10.0",
        "mongoose": "^5.2.5",
        "request": "^2.87.0",
        "request-promise-native": "^1.0.5",
        "validator": "^10.4.0"
    },
    "now": {
        "name": "auth",
        "alias": "auth.cp-api.tech",
        "env": {
            "JWT_SECRET": "@jwt_secret",
            "DB_URL": "@db_url",
            "CAPTCHA_SECRET_KEY": "@captcha_secret_key"
        }
    },
    "devDependencies": {
        "micro-dev": "^3.0.0"
    }
}
